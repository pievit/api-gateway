services:
  api-gateway:
    build:
      context: ./api-gateway/
    container_name: api-gateway
    image: pievit/api-gateway:latest
    ports:
      - "8080:8080"
    networks:
      - opnet

networks:
  opnet:
    driver: bridge
